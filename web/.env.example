# Base URL for the Asset Management C# Web API.
# In dev, point at the local API; in prod the docker-compose network alias.
VITE_API_BASE_URL=http://localhost:5000/api

# Logto integration — placeholders for when the IAM module lands.
# VITE_LOGTO_ENDPOINT=
# VITE_LOGTO_APP_ID=
# VITE_LOGTO_RESOURCE=
